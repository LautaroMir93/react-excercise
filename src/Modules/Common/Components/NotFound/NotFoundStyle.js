import styled from 'styled-components'
import { Col } from 'antd'

export const NotFoundWrapper = styled(Col)`
  margin-top: 50px;
  text-align: center;
  h1 {
    font-size: 50px;
  }
`

export default NotFoundWrapper
